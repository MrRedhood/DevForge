name: Bootstrap DevForge source import

on: push

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    steps:
      - name: Check Actions execution
        run: echo "DevForge bootstrap runner is active"
